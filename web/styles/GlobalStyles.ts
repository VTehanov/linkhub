import { createGlobalStyle } from 'styled-components'

const GlobalStyles = createGlobalStyle`
  @import url('https://fonts.googleapis.com/css?family=Libre+Franklin|Montserrat:400,700|Nunito+Sans');

  body {
    margin: 0;
    padding: 0;
  }
`

export default GlobalStyles
