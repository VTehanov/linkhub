# import Project from '../../shared/Project.graphql'

input GetProjectInput {
  slug: String!
}

type GetProjectResponse {
  project: Project
}

type Query {
  getProject(input: GetProjectInput!): GetProjectResponse
}
